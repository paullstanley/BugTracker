import XCTest
@testable import iOS_ApplicationLogic

final class iOS_ApplicationLogicTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(iOS_ApplicationLogic().text, "Hello, World!")
    }
}
