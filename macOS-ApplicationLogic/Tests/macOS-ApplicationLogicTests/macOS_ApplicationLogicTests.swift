import XCTest
@testable import macOS_ApplicationLogic

final class macOS_ApplicationLogicTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(macOS_ApplicationLogic().text, "Hello, World!")
    }
}
